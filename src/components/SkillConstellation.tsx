import React from 'react'

export default function SkillConstellation(){
  return (
    <div className="glass p-4" style={{minHeight:200}}>
      <div className="text-sm text-muted mb-2" style={{color:'var(--muted)'}}>Skill Constellation</div>
      <div className="text-sm">Interactive network visualization placeholder — replace with D3/Three implementation.</div>
    </div>
  )
}
import React from 'react'

export default function SkillConstellation(){
  return (
    <div className="glass p-4" style={{minHeight:200}}>
      <div className="text-sm text-muted mb-2" style={{color:'var(--muted)'}}>Skill Constellation</div>
      <div className="text-sm">Interactive network visualization placeholder — replace with D3/Three implementation.</div>
    </div>
  )
}
